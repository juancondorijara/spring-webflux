server:
  port: 8081

spring:
  application:
    name: spring-webflux-nosql
  data:
    mongodb:
      database: DATABASE
      uri: mongodb+srv://USER:PASSWORD@cluster0.zdl9n.mongodb.net/?appName=Cluster0   #MongoDB Atlas (Cluod)
      #uri: mongodb://USER:PASSWORD@localhost:27017/?authSource=admin   #MondoDB Container (Local)

springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html  # http://localhost:8081/swagger-ui.html